//import "test.code";

name := 25;

func double(n : int) : int {
	return n * 2;
}

func main() {
	name = double(name);
	defer name += 2;

	for i := 0, i < 25, i += 1 {
		name += 2;
	}
}

hello: = "hey";

main();

/*
func double_pointer(num : ptr int) {
	deref num *= 2;
}

pointer_to_int : ptr int = alloc 25;
free pointer_to_int;

double_pointer(pointer_to_int);

second_pointer := ref name;
deref second_pointer += 2; // 'name' will now have 2 added.
*/